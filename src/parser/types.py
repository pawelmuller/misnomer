from enum import Enum, auto


class Type(Enum):
    # Types
    INT = auto()                # int
    FLOAT = auto()              # float
    STRING = auto()             # string
    NOTHING = auto()            # nothing
